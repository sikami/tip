JAVA


String


untuk perbandingan use:
.equals("test");

untuk split string:
text.split(",");

utk menemukan character at specified index
charAt(index)

--ex:
String text = "Hello world!";
char character = text.charAt(0);
System.out.println(character);


length()

String word = "equisterian";
int length = word.length();
> 11

to delete last character of a string use:

String.sunstring(beginIndex, endIndex);

to delete trailing white space of  string ex: "Hello World  "
use trim()

